/**
 * Page-level container layout class shared across sections.
 * Use via the `Container` component, or directly when you need to apply it on
 * an element that doesn't justify a wrapper (e.g. nested grids).
 */
export const containerClass =
  'mx-auto w-full max-w-[1280px] px-5 sm:px-8 lg:px-10'
